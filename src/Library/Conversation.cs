using System;
using System.Collections.Generic;
/*
namespace Library
{
    /// <summary>
    /// Clase que permite establecer una conversación entre dos jugadores.
    /// </summary>
    public class Conversation
    {
       /// <summary>
       /// Jugador que manda el mensaje.
       /// </summary>
       private User playerSend;
       /// <summary>
       /// Jugador que recive el mensaje.
       /// </summary>
       private User playerReceive;

       /// <summary>
       /// Lista de mensajes que se mandaron ambos??
       /// </summary>
       /// <typeparam name="Text">Mensajes</typeparam>
       /// <returns></returns>
       private List<Text> text = new List<Text> ();

       /// <summary>
       /// Metodo para enviar mensajes
       /// </summary>
       /// <param name="playerSend">Jugador que envia el mensaje.</param>
       /// <param name="playerReceive">Jugador que recibe el mensaje</param>
       /// <param name="text">Mensaje</param>
       public void SendMessage(User playerSend, User playerReceive, Text text)
       {
           playerReceive.Receive(playerSend,text);
       }
    }
}

*/